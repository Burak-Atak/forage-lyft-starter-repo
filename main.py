from models.car import *
from models.car_model import *


calliope = Car(model=Calliope(), service_record=ServiceRecord())
glissade = Car(model=Glissade(), service_record=ServiceRecord())
palindrome = Car(model=Palindrome(), service_record=ServiceRecord())
rorschach = Car(model=Rorschach(), service_record=ServiceRecord())
thovex = Car(model=Thovex(), service_record=ServiceRecord())
